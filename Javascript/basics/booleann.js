// === equality operation
// ==! no equal operator
// < less than operator
//<= less equal
// > greater than operator
// >= greater equal

// Chalenge

//Create age set to your age
//Calculate is child - if they are 7 or under
//Calculate is senior - if the are 65 or older
//Print is child value
//Print if senir value

let yourAge = 7;
let isChild = yourAge <= 7;
let isSenior = yourAge >= 65;

console.log(isChild);
console.log(isSenior);